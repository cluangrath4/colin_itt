#pragma once

// This is the only header needed by users of the library
// I think this shpould be the only header that users need to include because we only need the ITTAPI function declarations
#include "ittnotify.h"
